# Orrery unification (historical)

The Pixi/Three migration and root promotion described by earlier versions of this
document are complete. The maintained design is documented in the
[current architecture](architecture.md), with detailed contracts for
[rendering and options](rendering.md), [catalogue loading](catalog-loading.md) and
the [Three adapter](three-renderer.md).

This path remains as a compatibility target for external links. The completed
rollout plan, source mapping and implementation-unit history remain available in
Git history:

```sh
git log --follow -- docs/unification.md
```

Release evidence and rollback guidance are kept in
[release and rollback](promotion.md).
